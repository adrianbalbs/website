---
import Layout from "../layouts/Layout.astro";
import "../styles/global.css";

// Welcome to Astro! Wondering what to do next? Check out the Astro documentation at https://docs.astro.build
// Don't want to use any of this? Delete everything in this file, the `assets`, `components`, and `layouts` directories, and start fresh.
// TODO: headings need specific font
//

const contentWidth = "800px";
---

<Layout>
    <div id="container">
        <header id="site-nav">
            <nav>
                <ul>
                    <li id="site-nav-home">
                        <a href="">Adrian Balbalosa</a>
                    </li>
                    <li class="site-nav-divider" role="separator"></li>
                    <li>
                        <a href="">Blog</a>
                    </li>
                    <li>
                        <a href="">Links</a>
                    </li>
                </ul>
            </nav>
        </header>
        <main>
            <h1>Adrian Balbalosa</h1>
            <p>Some text</p>
            <a href="https://nuphy.com/?sca_ref=11306305.p2hPK9CKOM">Link</a>
        </main>
    </div>
</Layout>

<style define:vars={{ contentWidth }}>
    #container {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
    }

    header {
        position: sticky;
        z-index: 999;
        border-radius: 2.25rem;
        margin-top: var(--spacing-4);
        background-color: var(--gray);
        backdrop-filter: var(--blur);
        box-shadow: var(--edge-highlight), var(--shadow-glass);
        padding: var(--spacing-3) var(--spacing-4);
    }

    #site-nav ul {
        display: flex;
        gap: var(--spacing-1);
        list-style: none;
        margin: 0;
        padding-left: 0px;
        align-items: center;
    }

    #site-nav a {
        display: inline-block;
        border-radius: 2.25rem;
        color: var(--fg-muted);
        text-decoration: none;
        padding: var(--spacing-1) var(--spacing-2);
        transition:
            background-color 150ms ease,
            color 150ms ease;
    }

    #site-nav a:hover {
        background-color: var(--accent);
        color: black;
    }

    .site-nav-divider {
        width: 1px;
        align-self: stretch;
        margin-inline: var(--spacing-2);
        background-color: color-mix(in hsl, var(--fg) 14%, transparent);
    }

    #site-nav-home {
        border-radius: 2.25rem;
        background-color: var(--gray-accent);
        box-shadow: var(--edge-highlight), var(--shadow-glass);
        padding: var(--spacing-1) var(--spacing-2);
        transition: background-color 150ms ease;
    }

    #site-nav-home:hover {
        background-color: var(--accent);
    }

    #site-nav-home a {
        color: var(--accent);
    }

    #site-nav-home:hover a,
    #site-nav-home a:hover {
        background-color: transparent;
        color: black;
    }

    main {
        width: var(--contentWidth);
        max-width: var(--contentWidth);
        margin-top: 68px;
    }
</style>
